# Running App in Debug Mode

## ✅ App is Running in Debug Mode

The app is now running with debugging enabled on your phone.

---

## 🔍 Debug Features Enabled

When running in debug mode:
- ✅ **Hot Reload** - Press `r` in terminal to reload changes
- ✅ **Hot Restart** - Press `R` in terminal to restart
- ✅ **Debug Console** - See logs and errors in terminal
- ✅ **Performance Monitoring** - Track app performance
- ✅ **Breakpoints** - Can be set in VS Code/Android Studio

---

## 📱 What's Happening

1. **App is building** in debug mode
2. **Will install** on your phone (A001)
3. **Will launch automatically**
4. **Debugging enabled** - you can see logs in terminal

---

## ⌨️ Debug Commands (While App is Running)

Once the app is running, you can use these commands in the terminal:

- **`r`** - Hot reload (apply code changes instantly)
- **`R`** - Hot restart (restart the app)
- **`h`** - List all available commands
- **`q`** - Quit and stop the app
- **`d`** - Detach from debugger (keep app running)

---

## 🐛 Debugging Tips

### View Logs:
- All logs appear in the terminal where you ran `flutter run`
- Look for errors, warnings, and debug messages

### Set Breakpoints:
- In VS Code: Click left of line numbers to set breakpoints
- In Android Studio: Click in the gutter to set breakpoints

### Inspect Variables:
- Use `print()` statements in your Dart code
- Check terminal output for your debug messages

---

## 📋 Check Your Phone

- ✅ App should be installing/launching
- ✅ Look for "fillora" app
- ✅ App will open automatically
- ✅ You can test Google Sign-In

---

**The app is running in debug mode with full debugging capabilities!** 🐛✨

